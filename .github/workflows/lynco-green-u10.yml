name: LynCo U10 GREEN refresh
on:
  push:
    branches: [qualification/green-u10-a8c2de5]
permissions:
  contents: read
jobs:
  u10:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: ./.github/actions/setup-node-pnpm
        with:
          node-version: "24"
      - run: node --test scripts/__tests__/build-missing-incremental-output.test.mjs
