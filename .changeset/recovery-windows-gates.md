---
"@runfusion/fusion": patch
---

summary: Run static merge checks correctly from spaced Windows checkouts.
category: fix
dev: Use file URL identity for executable entrypoint detection and argument-based Git
enumeration. Keep source gate bundles loadable under Vitest and make Windows test
path handling reflect the same production invariants as POSIX fixtures.
