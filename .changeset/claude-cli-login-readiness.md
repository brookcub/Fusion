---
"@runfusion/fusion": patch
---

summary: Require confirmed native Claude CLI login before reporting the provider authenticated or ready.
category: fix
dev: Share native child-auth policy with bounded path, version, and JSON login probes; keep binary availability separate and fail closed without changing paused task state.
